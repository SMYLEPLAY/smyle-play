# SMYLE PLAY — Source des dépendances (P3 lockfile, 2026-07-30).
# Ce fichier tient les CONTRAINTES ; requirements.txt est GÉNÉRÉ (épinglé) :
#   uv pip compile requirements.in --python-version 3.11 -o requirements.txt
# Ne jamais éditer requirements.txt à la main.

# ── ASGI / FastAPI ──────────────────────────────────────────────────────────
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.27.0,<1.0

# ── DB async ────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29.0,<1.0
alembic>=1.13.0,<2.0

# ── Pydantic / config ───────────────────────────────────────────────────────
pydantic[email]>=2.0,<3.0
pydantic-settings>=2.0,<3.0

# ── Auth / crypto ───────────────────────────────────────────────────────────
PyJWT>=2.8.0,<3.0
bcrypt>=4.0.0,<6.0

# ── HTTP / utils ────────────────────────────────────────────────────────────
httpx>=0.26.0,<1.0
python-multipart>=0.0.9,<1.0
structlog>=24.1.0,<27.0
sentry-sdk[fastapi]>=1.40.0,<3.0
slowapi>=0.1.9,<1.0

# ── Config / env ────────────────────────────────────────────────────────────
python-dotenv>=1.0.1,<2.0

# ── Cloudflare R2 ───────────────────────────────────────────────────────────
boto3>=1.34.0,<2.0

# ── Audio (previews, durée) ─────────────────────────────────────────────────
pydub>=0.25.1,<1.0

# ── Images ──────────────────────────────────────────────────────────────────
Pillow>=10.4.0,<13.0
