# SMYLE PLAY — Dépendances Python (FastAPI — Flask supprimé, P0-c 2026-07-30)
# Installation : pip3 install -r requirements.txt
#
# Durcissement 2026-07-24 : bornes SUPÉRIEURES (< majeure suivante) pour
# empêcher un bump majeur amont de casser la prod sans changement de code.
# Les bornes sont posées juste au-dessus des versions actuellement installées
# et testées (aucun downgrade). Un vrai lockfile viendra plus tard.

# ── ASGI / FastAPI (stack moderne) ────────────────────────────────────────────
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.27.0,<1.0

# ── DB async (FastAPI) ────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29.0,<1.0
alembic>=1.13.0,<2.0

# ── Pydantic / config ─────────────────────────────────────────────────────────
pydantic[email]>=2.0,<3.0
pydantic-settings>=2.0,<3.0

# ── Auth / crypto ─────────────────────────────────────────────────────────────
PyJWT>=2.8.0,<3.0
bcrypt>=4.0.0,<6.0

# ── HTTP / utils ──────────────────────────────────────────────────────────────
httpx>=0.26.0,<1.0
python-multipart>=0.0.9,<1.0
structlog>=24.1.0,<27.0
sentry-sdk[fastapi]>=1.40.0,<3.0
slowapi>=0.1.9,<1.0            # rate-limiting auth + achats (Tier 1 sécurité)

# ── Config / env ──────────────────────────────────────────────────────────────
python-dotenv>=1.0.1,<2.0

# ── Cloudflare R2 ─────────────────────────────────────────────────────────────
boto3>=1.34.0,<2.0

# ── Audio processing (preview 30s voix) ───────────────────────────────────
pydub>=0.25.1,<1.0

# ── Images (C4 Monde Visuel) ────────────────────────────────────────────────
# Génération d'aperçu réduit (≤1024px, JPEG q80) à l'upload d'une image IA.
# NB : surveiller mémoire/timeout sur Railway pour les gros PNG.
Pillow>=10.4.0,<13.0
