# ── Fichiers audio (hébergés sur Cloudflare R2, pas dans Git) ──────────────
*.wav
*.mp3
*.flac
*.aac
*.ogg
*.m4a

# ── Ableton Session Data (jamais versionné) ──────────────────────────────
*.asd

# ── Dossier Claude local (config + worktrees) ────────────────────────────
.claude/

# ── Assets audio (2+ Go, hébergés sur Cloudflare R2, pas dans Git) ───────
assets_audio/
# (anciens dossiers racine, conservés au cas où pour éviter un re-tracking)
HIT MIX/
NIGHT CITY/
SUNSET LOVER/
JUNGLE OSMOSE/

# ── Dossier de triage Suno Router (fichiers en attente de classement) ────
A_CLASSER/*
!A_CLASSER/.gitkeep

# ── macOS ──────────────────────────────────────────────────────────────────
.DS_Store
._*
.Spotlight-V100
.Trashes

# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.env
.env.local

# ── Dossier FICHIER (backup historique, pas besoin de versionner) ─────────
FICHIER/

# ── Base de données locale ────────────────────────────────────────────────
*.db
*.sqlite
*.sqlite3

# ── Logs ──────────────────────────────────────────────────────────────────
*.log
logs/
.watcher-logs/

# ── Éditeurs ──────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp

# ── Graphify — code-graph build artifacts (rebuilt on demand) ─────────────
graphify-out/

# ── Vault Obsidian — whitelist sélective (2026-05-11) ────────────────────
# Tout OBSIDIAN/ est exclu par défaut, SAUF les sous-dossiers source de vérité.
# Notes créatives WATT, archives et tarification restent local-only.
Smyle-play/
OBSIDIAN/*
!OBSIDIAN/00_INDEX.md
!OBSIDIAN/01_PRODUIT/
!OBSIDIAN/03_SESSIONS/
!OBSIDIAN/04_TASKS/
!OBSIDIAN/05_TECH/
# Ré-exclusions ciblées (sensibles, restent local)
OBSIDIAN/01_PRODUIT/Tarification_v1.md
OBSIDIAN/05_TECH/Legacy/

# ── data/exports/ — dumps DB et exports temporaires (jamais commit) ───────
data/exports/*
!data/exports/.gitkeep
!data/exports/README.md

# ── Backups éditeur ──────────────────────────────────────────────────────
*.bak
*.swp
*.swo
*~

# ── Environnements virtuels Python ───────────────────────────────────────
venv/
.venv/
env/
smyleplay-api/venv/
smyleplay-api/_archive_bak/

# ── Secrets (NE JAMAIS COMMIT) ───────────────────────────────────────────
.env
.env.local
.env.production
*.pem
*.key

# ── IDE ──────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.iml

# ── Node (si front futur) ────────────────────────────────────────────────
node_modules/

# ── Logs ─────────────────────────────────────────────────────────────────
*.log

*.sql
*.dump
